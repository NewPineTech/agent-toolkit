# Google Drive
google-api-python-client>=2.108.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1

# VLM (Gemini for OCR)
google-generativeai>=0.8.0

# LLM (OpenAI for form metadata)
openai>=1.40.0

# RAGFlow API
requests>=2.31.0

# File reading
python-docx>=1.1.0          # for .docx
xlrd>=2.0.1                 # for legacy .xls
openpyxl>=3.1.2             # for .xlsx
pandas>=2.1.0

# Image processing
Pillow>=10.0.0

# Utilities
PyYAML>=6.0
tqdm>=4.66.0
python-slugify>=8.0.1
tenacity>=8.2.3              # retry logic

# Markdown → PDF (Step 3.5 - optional)
markdown>=3.5                # Markdown → HTML
weasyprint>=60.0             # HTML → PDF (pure Python, Unicode support)
